---
  - become: true
    hosts: all
    tasks:
      - name: "deploy docker image"
        shell: "docker rm -f java-container"
        shell: "docker rmi artibhoir369/java-app"
        shell: "docker run -itd -p 80:8080 --name java-container artibhoir369/java-app"